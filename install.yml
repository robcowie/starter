---
# Here we describe every software we may want to install
# TODO: Move mjolnir to a role (with lua and lou

- hosts: all
  roles:
  - role: robcowie.vim
  - role: williamyeh.oracle-java
  - role: osxc.packages
    brew_taps:
      - caskroom/fonts
      - homebrew/versions

    brew_packages:
      - git
      - hub
      - zsh
      #- vim
      - tmux
      - awscli
      - direnv
      - fasd
      - lua
      - pgcli
      - pv
      - task

    cask_packages:
      - iterm2
      - bittorrent-sync
      - slack
      - vlc
      - sequel-pro
      - keepingyouawake
      - flux
      - hipchat
      #- logitech-control-center
      - logitech-options
      - mjolnir
      - trailer
      - alfred
      - fantastical
      - telegram
      - viscosity
      - font-source-code-pro
      - dropbox
      - google-cloud-sdk
      #- homebrew/versions/apache-spark141
      #- apache-spark
      - safari-technology-preview